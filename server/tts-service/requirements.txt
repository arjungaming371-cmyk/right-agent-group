# IndicF5 pulls torch, torchaudio, and the F5-TTS stack transitively.
git+https://github.com/ai4bharat/IndicF5.git
# Pinned: newer transformers inits custom models on the meta device, which
# breaks IndicF5's __init__ ("Tensor.item() cannot be called on meta tensors").
transformers==4.47.1
soundfile
fastapi
uvicorn[standard]
