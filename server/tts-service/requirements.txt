# Edge TTS — Microsoft neural voices, CPU-only, no GPU needed.
edge-tts

# pydub (and the audioop-lts shim it needed on Python 3.13+) is gone: it only
# ever existed to stitch separately-synthesized clips together, and this
# service no longer splits a reply into clips at all — one request, one voice,
# one continuous utterance. See app.py's module docstring.

fastapi
uvicorn[standard]
