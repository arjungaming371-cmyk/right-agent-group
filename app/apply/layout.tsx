import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Meet Priya — AI Voice Agent for Lending",
  description:
    "Hear Priya live, see the features and pricing, and apply for a setup: AI voice calls, WhatsApp automation and a full lead pipeline for lending teams.",
}
export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return children
}
